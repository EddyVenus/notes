console.log(window);    // Lado del navegador
console.log(global);    // Lado de Node
console.log(self);      // WebWorker
console.log(globalThis);

// Objeto global: Contiene los métodos y propiedades globales para el programa.